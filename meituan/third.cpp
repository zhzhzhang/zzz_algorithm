#include<iostream>
#include<cmath>
using namespace std;

int main() {
    while(1) {
    int n;
    int total = (1+n)*n/2;
    int res = 0;
    for(int i = 0; i < n; i++) {
        res = 
    }
    string str;
    cin >> str;
    int len = str.size();
    int num = 0;
    for(int i = 0; i < len; i++) {
        if(str[i] < 91) num ++;
    }
    cout << abs(num-len/2) << endl;}
    return 0;
}